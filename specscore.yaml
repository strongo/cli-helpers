# SpecScore Repo Config Schema: https://specscore.md/repo-config

project:
    title: cli-helpers — shared helpers for Go CLIs
    host: github.com
    org: strongo
    repo: cli-helpers

# Plans live in this repository (same-repository Plan storage).
plans_repo: strongo/cli-helpers
